# Ensure devDependencies (vite, typescript, etc.) are installed even when NODE_ENV=production
# so that `npm run build` works after a fresh `npm install`.
# Do not omit optional deps - rollup needs @rollup/rollup-linux-x64-gnu on Linux (npm bug #4828)
